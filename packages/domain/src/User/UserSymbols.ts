enum UseCases {
  GetUser = 'GetUserUseCase',
}

enum Services {
  GetUser = 'GetUserService',
}

enum Repositories {
  User = 'UserRepository',
}

export const UserSymbols = {
  UseCases,
  Services,
  Repositories,
};
