enum UseCases {
  Login = 'LoginUseCase',
}

enum Services {
  Login = 'LoginService',
}

enum Repositories {
  Login = 'LoginRepository',
}

export const UserSymbols = {
  UseCases,
  Services,
  Repositories,
};
