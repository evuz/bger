{
  "name": "bger",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "domain:build": "yarn workspace @bger/domain build",
    "domain:dev": "yarn workspace @bger/domain dev",
    "domain:test": "yarn workspace @bger/domain test",
    "domain:prettier": "yarn workspace @bger/domain prettier",
    "domain:lint": "yarn workspace @bger/domain lint",
    "prettier": "run-p domain:prettier",
    "lint": "run-p domain:lint"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^1.18.2"
  }
}
